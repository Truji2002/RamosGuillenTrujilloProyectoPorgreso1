@model IEnumerable<RamosGuillenTrujilloProyectoPorgreso1.Models.Promocion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCaducidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoPromocion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCaducidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPromocion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPromociones">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPromociones">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPromociones">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
